/**
 * Get the are of a triangle
 * @param {number} base 
 * @param {number} height 
 */
function areaTriang(base, height) {
    return (base * height) / 2;
}
//module.exports (Sirve para importar la funcion a otro archivo)

module.exports = areaTriang;

